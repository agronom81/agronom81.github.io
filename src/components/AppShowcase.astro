<section class="showcase-wrap" id="apps" aria-labelledby="apps-title">
	<div class="section-intro"><span class="eyebrow">Selected work</span><h2 id="apps-title">Tools that fit<br /><em>real life.</em></h2><p>Two small apps, each built around one useful idea.</p></div>
	<div class="app-grid">
		<a class="app-card medicine" href="/medicine-reminder">
			<div class="card-copy"><span class="tech-badge">React Native</span><h3>Medicine<br />Reminder</h3><p>Medication reminders made simple.</p><span class="view-link">View app <b>→</b></span></div>
			<div class="card-art"><img class="app-preview" src="/images/mr/app-1.webp" alt="Medicine Reminder app screen" loading="lazy" /></div>
		</a>
		<a class="app-card dish" href="/dish-log">
			<div class="card-copy"><span class="tech-badge">Flutter</span><h3>Dish<br />Log</h3><p>Remember what you cooked and discover what to make next.</p><span class="view-link">View app <b>→</b></span></div>
			<div class="card-art"><img class="app-preview" src="/images/dl/app-1.webp" alt="Dish Log app screen" loading="lazy" /></div>
		</a>
	</div>
</section>

<style>
	.showcase-wrap { width: min(calc(100% - 48px), var(--max-width)); margin: 0 auto; padding: 145px 0 35px; }
	.section-intro { display: grid; grid-template-columns: 1fr 1.5fr 1fr; align-items: end; gap: 25px; margin-bottom: 45px; }
	.eyebrow, .tech-badge { color: var(--react); font: 500 .68rem 'DM Mono', monospace; letter-spacing: .06em; text-transform: uppercase; }
	.section-intro h2 { margin: 0; font-size: clamp(2.7rem, 5vw, 5rem); line-height: .95; letter-spacing: -.08em; }
	.section-intro h2 em { color: var(--muted); font-style: normal; }
	.section-intro p { margin: 0 0 5px; color: var(--muted); font-size: .85rem; line-height: 1.6; }
	.app-grid { display: flex; gap: 10px; min-height: 470px; }
	.app-card { position: relative; display: flex; flex: 1 1 0; min-width: 0; overflow: hidden; padding: 42px; border: 1px solid rgba(255,255,255,.12); border-radius: 26px; transition: flex-grow .55s cubic-bezier(.2,.75,.25,1), border-color .4s ease, transform .4s ease; }
	.app-card:hover { flex-grow: 1.9; border-color: rgba(255,255,255,.3); }
	.app-card.medicine { background: radial-gradient(circle at 78% 24%, rgba(97,218,251,.22), transparent 32%), linear-gradient(135deg, #111923, #101319); }
	.app-card.dish { background: radial-gradient(circle at 78% 24%, rgba(19,185,253,.2), transparent 32%), linear-gradient(135deg, #121a23, #11151b); }
	.card-copy { position: relative; z-index: 1; display: flex; flex: 1; min-width: 185px; flex-direction: column; align-items: flex-start; }
	.dish .tech-badge { color: var(--flutter); }
	.card-copy h3 { margin: 50px 0 18px; font-size: clamp(2rem, 4vw, 4.3rem); line-height: .91; letter-spacing: -.09em; }
	.card-copy p { max-width: 190px; margin: 0; color: var(--muted); font-size: .82rem; line-height: 1.55; }
	.view-link { margin-top: auto; color: var(--text); font-size: .78rem; }
	.view-link b { display: inline-block; margin-left: 8px; color: var(--react); font-size: 1.1rem; transition: transform .3s ease; }
	.dish .view-link b { color: var(--flutter); }
	.app-card:hover .view-link b { transform: translateX(5px); }
	.card-art { display: grid; flex: 1; min-width: 100px; place-items: center; opacity: .85; transition: transform .5s ease, opacity .5s ease; }
	.app-preview { display: block; width: 148px; height: 270px; max-width: 100%; object-fit: contain; transform: rotate(7deg) scale(.86); transition: transform .5s cubic-bezier(.2,.75,.25,1); }
	.app-card:hover .card-art { opacity: 1; transform: scale(1.08); }
	.app-card:hover .app-preview { transform: rotate(0deg) translateY(-8px) scale(.86); }
	@media (max-width: 800px) { .section-intro { grid-template-columns: 1fr 1fr; } .section-intro p { grid-column: 2; } .app-grid { min-height: 400px; } .app-card { padding: 30px; } }
	@media (max-width: 620px) { .showcase-wrap { width: min(calc(100% - 32px), var(--max-width)); padding-top: 105px; } .section-intro { display: block; } .section-intro h2 { margin: 20px 0 15px; } .section-intro p { max-width: 230px; } .app-grid { display: block; } .app-card { min-height: 360px; margin-bottom: 10px; padding: 28px; } .app-card:hover { flex-grow: 1; } .card-copy h3 { margin-top: 42px; } }
</style>
